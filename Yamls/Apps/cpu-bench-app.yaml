apiVersion: v1
kind: ServiceAccount
metadata:
  name: cpu-bench
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpu-bench
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1              
  selector:
    matchLabels:
      app: cpu-bench
      version: v1
  template:
    metadata: 
      labels:
        app: cpu-bench
        version: v1
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: DoesNotExist
      containers:
          #Note for kind to load and use the local image, set the tag different to latest or use PullPolicy IfNotPresent.
        - name: cpu-bench
          image: cpu-bench
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
      terminationGracePeriodSeconds: 1
---
apiVersion: v1
kind: Service
metadata:
  name: cpu-bench-service
  labels:
    app: cpu-bench
    service: cpu-bench
spec:
  selector:
    app: cpu-bench
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http-cpu
