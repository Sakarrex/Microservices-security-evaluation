apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: front-mtls
  namespace: default
spec:
  selector:
    matchLabels:
      app: front
  mtls:
    mode: STRICT
---
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: cpu-bench-mtls
  namespace: default
spec:
  selector:
    matchLabels:
      app: cpu-bench
  mtls:
    mode: STRICT
---
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: mem-bench-mtls
  namespace: default
spec:
  selector:
    matchLabels:
      app: mem-bench
  mtls:
    mode: STRICT
