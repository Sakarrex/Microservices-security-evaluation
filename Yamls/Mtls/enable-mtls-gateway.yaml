apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: enable-gateway-mtls
  namespace: default
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: app-gateway
  mtls:
    mode: STRICT